definitions:
  Clients:
    type: object
    properties:
      id:
        type: string
      nom:
        type: string
      prenom:
        type: string
responses:
  200:
    description: liste des clients
    schema:
      $ref: '#/definitions/Clients'
    examples:
      clients: ['id:1', 'nom: XXXX', 'prénom:YYYYY']